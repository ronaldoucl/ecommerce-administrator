// Controllers layer.
// Responsibility: thin HTTP adapters. Parse and validate the request shape, call the
// matching service, and shape the response (status code + JSON body). No business
// logic and no direct Prisma access here. Flow: routes -> controllers -> services -> Prisma.
