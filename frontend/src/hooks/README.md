# hooks

Reusable custom React hooks.

Planned hooks (added in later tickets):

- `useCart` — thin accessor over **CartContext** (cart items, totals and the
  add/remove/update/clear actions).
- `useAuth` — thin accessor over **AuthContext** (auth status, token, and the
  `login` / `logout` actions used to guard the admin area).

_Placeholder for S1-RON-01 — no hooks implemented yet._
