# assets

Static assets bundled with the app (images, logos, icons, fonts).

Design tokens (color palette + typography) live in [`../styles`](../styles) and
are applied globally through the Layout.

_Placeholder for S1-RON-01 — no assets added yet._
