/*
 * CartContext (placeholder for S1-RON-01).
 *
 * Will hold the shopping cart state and expose it to the storefront:
 *   - the list of cart items (product + quantity)
 *   - derived totals (item count, subtotal)
 *   - addItem, removeItem, updateQuantity and clearCart actions
 *
 * The Cart, Checkout and product pages will consume this context. State will
 * likely be persisted to localStorage. The real provider is implemented in a
 * later ticket.
 */

// TODO: implement createContext, CartProvider and a useCart hook.
export {};
