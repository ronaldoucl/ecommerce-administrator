# Base URL of the backend API. Must INCLUDE the /api prefix, because the service
# layer calls short paths on top of it (e.g. api.get('/products')).
# Local backend:  http://localhost:4000/api
# Production:     https://<your-render-service>.onrender.com/api
VITE_API_URL=http://localhost:4000/api
