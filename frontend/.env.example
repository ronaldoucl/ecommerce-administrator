# Copy this file to `.env` and set the value for your environment.
# `.env` is gitignored; `.env.example` is committed as documentation.

# Base URL of the backend API. Must include the /api prefix.
#   Local backend:    http://localhost:4000/api
#   Deployed backend: https://ecommerce-administrator-backend.onrender.com/api
VITE_API_URL=http://localhost:4000/api
